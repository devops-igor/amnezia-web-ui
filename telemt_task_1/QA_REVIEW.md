# QA REVIEW: Telemt Proxy Default Port Fix (443 → 18443)

**Verdict:** APPROVED

**Reviewer:** qa_bot (Hermes Agent)
**Date:** 2026-04-10
**Task:** Fix Telemt proxy links defaulting to port 443 instead of 18443

---

## Executive Summary

All three intended changes have been applied correctly. No unintended side effects or regressions detected. The fix properly changes the default external port from 443 to 18443 while preserving all internal container ports.

---

## Scanner Results

### Python Smoke Test
```bash
$ python3 -m py_compile telemt_manager.py
SMOKE TEST PASSED
Exit code: 0
```

### Static Analysis
- No Python linters configured in project (no `requirements.txt` for dev dependencies)
- Manual code review performed on all changed files and critical code paths

---

## Detailed Verification

### Changed Files (3/3) ✓

#### 1. telemt_manager.py (line 78)
- **Before:** `port="443"`
- **After:** `port="18443"`
- **Status:** ✓ Correct
- **Context:** Default parameter for `install_protocol()` method

#### 2. templates/server.html (lines 818-819)
- **Before:**
  ```javascript
  portInput.value = '443';
  portHint.textContent = 'Порт для Telegram-прокси (обычно 443)';
  ```
- **After:**
  ```javascript
  portInput.value = '18443';
  portHint.textContent = 'Порт для Telegram-прокси (обычно 18443)';
  ```
- **Status:** ✓ Correct
- **Context:** UI default when Telemt protocol is selected

#### 3. protocol_telemt/config.toml (line 28)
- **Before:** `public_port = 443`
- **After:** `public_port = 18443`
- **Status:** ✓ Correct
- **Context:** Template default for tg:// link generation

### Unchanged Files (5/5) ✓

All five critical internal port references remain unchanged at 443:

1. **protocol_telemt/config.toml line 32:** `port = 443` ✓
   - Internal daemon listen port — correct

2. **protocol_telemt/docker-compose.yml line 8:** `"443:443"` ✓
   - Internal container port mapping — correct

3. **protocol_telemt/Dockerfile lines 78, 105:** `EXPOSE 443 9090 9091` ✓
   - Documents internal container port — correct

4. **telemt_manager.py line 59:** `docker port ... 443` ✓
   - Queries internal container port — correct

5. **telemt_manager.py line 146:** `re.sub(r'"443:443"', f'"{port}:443"', ...)` ✓
   - Substitution preserves internal port (right side of colon) — correct

### Port Flow Analysis ✓

Verified end-to-end port propagation:

```
UI (server.html:18443) 
  → API (app.py: req.port) 
  → install_protocol(port="18443") 
  → patches docker-compose: "{port}:443" 
  → patches config.toml: public_port = {port} 
  → stored in server["protocols"]["telemt"]["port"] = "18443"
  → add_client(port="18443") → tg://proxy?port=18443
  → get_client_config(port="18443") → tg://proxy?port=18443
```

The port is correctly stored during installation (app.py line 1554) and retrieved for link generation (app.py lines 1480-1481).

### Regression Checks ✓

1. **Other protocols unaffected:**
   - Xray protocol still defaults to port 443 (server.html line 814)
   - AWG protocol still defaults to port 55424 (server.html line 823)

2. **No orphaned references:**
   - Search for "18443" returns only the 3 intended changes + documentation
   - Search for "443" in changed files shows only the 5 intended unchanged references

3. **Syntax validity:**
   - Python compilation successful
   - JavaScript syntax appears correct (no parsing errors)
   - TOML syntax valid (line 28 follows same pattern as line 32)

---

## Test Coverage Assessment

**Coverage Gap:** No automated tests exist for Telemt protocol installation or link generation.

**Manual Verification Performed:**
- Code path tracing from UI → API → storage → link generation
- Verified port parameter flows through all layers correctly
- Confirmed internal ports remain isolated from external port changes

**Recommendation:** Add unit tests for:
- `TelemtManager.install_protocol()` default port
- `TelemtManager.get_client_config()` link generation with custom port
- `TelemtManager.add_client()` link generation with custom port

---

## Risk Assessment

| Risk | Severity | Mitigation |
|------|----------|------------|
| Existing installations retain port 443 | Low | Expected behavior — stored port preserved |
| Users manually entering 443 | Low | UI default only — manual entry still works |
| Internal port conflicts | None | Internal port unchanged at 443 |
| Breaking API consumers | None | API accepts explicit port parameter |

---

## Conclusion

The changes are minimal, focused, and correct. All acceptance criteria from TASK.md are met:

- [x] New Telemt installations default to external port 18443
- [x] `tg://proxy` links generated by add_client use port 18443 by default
- [x] API-generated links (from /v1/users) use public_port 18443 by default
- [x] Internal container port remains 443 (unchanged)
- [x] Existing installations are not broken (stored port preserved)

**Verdict: APPROVED** — Changes are safe for deployment.
